name: flutter_user_credentials
description: A flutter package designed to handle user credentials and provide an easy way of storing and retrieving saved credentials.
version: 0.0.1
homepage: https://github.com/nickolasdeluca/flutter_user_credentials

environment:
  sdk: '>=2.18.4 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.15

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
